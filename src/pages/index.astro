---
export const prerender = false;

import BaseLayout from '@/layouts/BaseLayout.astro';
import Hero from '@/components/sections/Hero.astro';
import {
  trendingSeries,
  bestMovies,
  mustWatchSeries,
  genres,
  recentReviews,
  faqItems,
  categories,
} from '@/mocks';
---

<BaseLayout
  title="Clak TV — Películas y series que debes ver"
  description="Descubre las películas y series más populares. Lleva tu historial, escribe reseñas y encuentra tu próximo favorito. El diario personal de todo lo que ves."
>
  <Hero />
  {/* TODO: Trending — {trendingSeries.length} series */}
  {/* TODO: Best Movies — {bestMovies.length} películas */}
  {/* TODO: Must Watch Series — {mustWatchSeries.length} series */}
  {/* TODO: Genres — {genres.length} géneros */}
  {/* TODO: Recent Reviews — {recentReviews.length} reseñas */}
  {/* TODO: FAQ — {faqItems.length} preguntas */}
  {/* TODO: Categories — {categories.length} categorías */}
  {/* TODO: Info CTA */}
</BaseLayout>
